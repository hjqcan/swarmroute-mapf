<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <!-- Frozen cross-context contract. BCL-only: must not depend on any context (esp. not Map). -->
</Project>
