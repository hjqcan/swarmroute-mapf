namespace SwarmRoute.TrafficControl.Application.Contract;

// Project shell created by Foundation/Platform (WS0). Squad-owned; fill in real types here.
// ITrafficCoordinatorAppService.TryReserve/Release -> AllocationOutcome. ReservationService implements IReservationView (Kernel) consumed by Planning. ITrafficControlSnapshotProvider (for Deadlock).
internal static class _Placeholder { }
